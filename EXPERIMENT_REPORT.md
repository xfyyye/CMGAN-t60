# KAN 多任务实验报告：T60 估计 + 去噪联合训练

## 1. 背景与动机

前期实验已验证 **KAN head + 单任务 T60 估计** 的有效性（最优单任务 RMSE = 107.70 ms）。
本轮实验在此基础上引入**去噪重建**作为辅助任务，探究两个问题：

1. 去噪任务能否为共享 Encoder 提供额外的正则化，从而改善 T60 估计性能？
2. 不同的任务损失权重比例（α·denoise + β·T60）对两个任务分别有何影响？

---

## 2. 模型架构变化

### 2.1 单任务 vs 多任务架构对比

```
单任务 T60 估计
───────────────────────────────────────────────────────
输入 wav (B, 64000)
  └─► STFT (n_fft=400, hop=100) ──► (B, 2, T=641, F=201)
        └─► power_compress        ──► (B, 2, T, F)
              └─► DenseEncoder    ──► (B, 64, T, F/2)
                    └─► TSCB × 2  ──► (B, 64, T, F/2)
                          └─► T60Head (FourierKAN) ──► T60 预测 (B,)

多任务（本轮）
───────────────────────────────────────────────────────
输入 wav (B, 64000)
  └─► STFT / power_compress       ──► (B, 2, T, F)
        └─► DenseEncoder          ──► (B, 64, T, F/2)   ← 共享
              └─► TSCB × 2       ──► (B, 64, T, F/2)   ← 共享
                    ├─► T60Head (FourierKAN) ──► T60 预测 (B,)      [主任务]
                    ├─► MaskDecoder         ──► 幅度掩码 (B,1,T,F)  [辅助任务]
                    └─► ComplexDecoder      ──► 复数残差 (B,2,T,F)  [辅助任务]
                          └─► iSTFT + power_uncompress ──► 增强音频
```

### 2.2 参数量变化

| 组件 | 单任务 | 多任务 |
|---|---|---|
| DenseEncoder | 约 260K | 约 260K（共享，不变） |
| TSCB × 2 | 约 516K | 约 516K（共享，不变） |
| T60Head (FourierKAN) | 约 86K | 约 86K（不变） |
| 去噪解码器（MaskDecoder + ComplexDecoder） | — | 约 0.54M（新增，合计 543,761） |
| **总参数量** | **约 862K（861,793）** | **约 1,406K（1,405,554，+544K，+63%）** |

参数量增加主要来自两个去噪解码器（CMGAN dense 解码器，镜像 encoder 结构，合计约 0.54M，+63%）；共享主干（encoder + 2 TSCB ≈ 0.78M）由两任务复用。

---

## 3. 损失函数设计

### 3.1 总损失

$$\mathcal{L} = \alpha \cdot \mathcal{L}_{\text{denoise}} + \beta \cdot \mathcal{L}_{\text{T60}}$$

**去噪损失**（$\mathcal{L}_{\text{denoise}}$）= 频域 RI 损失 + 幅度损失 + 时域损失：

$$\mathcal{L}_{\text{denoise}} = w_{ri} \cdot \mathcal{L}_{RI} + w_{mag} \cdot \mathcal{L}_{mag} + w_{time} \cdot \mathcal{L}_{time}$$

其中 $w_{ri}=0.1,\ w_{mag}=0.9,\ w_{time}=0.2$，去噪 target 为 `*_denoised.wav`（含混响、无噪声）。

**T60 损失**（$\mathcal{L}_{\text{T60}}$）= MSE，作用在归一化后的 T60 预测值上。

### 3.2 四组实验配置

| 实验名 | α (denoise) | β (T60) | 设计意图 |
|---|---|---|---|
| `w_denoise` | 1.0 | 0.1 | 去噪主导：验证强去噪监督能否正则化共享表征，反向提升 T60 |
| `w_balanced` | 0.5 | 0.5 | 均衡权重：两任务信号强度相近 |
| `w_eq` | 1.0 | 1.0 | 等权基线：两任务权重完全相等 |
| `w_t60` | 0.1 | 1.0 | T60 主导：验证去噪信号极弱时对 T60 的影响 |

**实验逻辑**：四组对照旨在回答"共享 Encoder 的梯度由谁主导"对两任务各自性能的影响。
梯度探针（每 20 步记录 denoise 与 T60 梯度的余弦相似度和幅度比 r）用于理解内部机制。

---

## 4. 整体平均结果（test1–test4 均值）

### 4.1 T60 估计指标

| 模型 | RMSE (ms) ↓ | MAE (ms) ↓ | Pearson r ↑ | R² ↑ |
|---|---|---|---|---|
| 单任务（KAN MSE，消融基线 kan_v2_mse） | 111.15 | 72.40 | 0.9484 | 0.8931 |
| 多任务 w_t60（α=0.1, β=1.0） | 107.84 | 62.79 | 0.9489 | 0.8993 |
| 多任务 w_eq（α=1.0, β=1.0） | 105.64 | 62.04 | 0.9520 | 0.9032 |
| 多任务 w_balanced（α=0.5, β=0.5） | 100.28 | 62.12 | 0.9569 | 0.9128 |
| **多任务 w_denoise（α=1.0, β=0.1）** | **96.41** | **58.69** | **0.9594** | **0.9195** |

w_denoise 相比单任务消融基线（kan_v2_mse，111.15 ms），RMSE 降低 **13.3%**（111.15 → 96.41 ms）。

> **外部对比**（test1–test4 均值 RMSE，详见 `result.txt`）：BERP 151.93 ms、noiseaware 133.66 ms、DAMTL 138.94 ms；本文 w_denoise 96.41 ms 显著领先三者。

### 4.2 去噪指标（w_denoise，test1–test4 均值）

| 指标 | 含噪输入（Noisy） | 增强后（Enhanced） | 提升量（Δ） |
|---|---|---|---|
| PESQ (wb) ↑ | 2.721 | **3.583** | +0.862 |
| STOI ↑ | 0.847 | **0.937** | +0.090 |
| SI-SDR (dB) ↑ | 15.91 | **21.83** | +5.92 |

> 注：去噪 target 为 `*_denoised.wav`（纯混响、无噪声），增强后的语音仍保留混响，
> 任务定义为**去噪而非去混响**。

### 4.3 四组实验去噪指标对比

| 实验 | PESQ ↑ | STOI ↑ | SI-SDR (dB) ↑ |
|---|---|---|---|
| w_t60（α=0.1, β=1.0） | 3.282 | 0.913 | 19.98 |
| w_balanced（α=0.5, β=0.5） | 3.410 | 0.924 | 20.86 |
| w_eq（α=1.0, β=1.0） | 3.466 | 0.929 | 21.22 |
| **w_denoise（α=1.0, β=0.1）** | **3.583** | **0.937** | **21.83** |

---

## 5. T60 分 bin 效果（0.1s 区间，test1–test4 均值）

| T60 区间 | 单任务(KAN MSE) RMSE | w_eq RMSE | w_t60 RMSE | **w_denoise RMSE** |
|---|---|---|---|---|
| 0.1–0.3 s | 87.2 ms | 76.6 ms | 93.0 ms | **63.5 ms** |
| 0.3–0.5 s | 100.9 ms | 72.2 ms | 87.8 ms | **63.7 ms** |
| 0.5–0.7 s | 106.5 ms | 84.3 ms | 95.0 ms | **96.2 ms** |
| 0.7–0.9 s | 105.1 ms | 93.4 ms | 100.6 ms | **94.9 ms** |
| 0.9–1.1 s | 131.3 ms | 126.6 ms | 115.4 ms | **123.5 ms** |
| 1.1–1.3 s | 123.5 ms | 124.4 ms | 107.8 ms | **116.7 ms** |
| 1.3–1.5 s | 241.4 ms | 320.6 ms | 260.5 ms | **251.0 ms** |

| T60 区间 | 单任务(KAN MSE) r | w_eq r | w_t60 r | **w_denoise r** |
|---|---|---|---|---|
| 0.1–0.3 s | 0.599 | 0.593 | 0.501 | **0.638** |
| 0.3–0.5 s | 0.525 | 0.581 | 0.551 | **0.635** |
| 0.5–0.7 s | 0.427 | 0.496 | 0.417 | 0.460 |
| 0.7–0.9 s | 0.390 | 0.522 | 0.491 | **0.489** |
| 0.9–1.1 s | 0.414 | 0.404 | 0.425 | 0.327 |
| 1.1–1.3 s | 0.446 | 0.607 | 0.509 | **0.515** |
| 1.3–1.5 s | 0.020 | −0.062 | 0.123 | **0.209** |

> w_denoise 在短 T60（≤0.5s）区间改善最显著；长 T60（≥0.9s）样本少、难度大，各模型均表现较弱。

---

## 6. SNR 分 bin 效果（test1–test4 均值）

### 6.1 T60 估计按 SNR 分 bin

| SNR | 单任务(KAN MSE) RMSE | w_eq RMSE | w_t60 RMSE | **w_denoise RMSE** |
|---|---|---|---|---|
| −5 dB | 188.3 ms | 163.2 ms | 191.7 ms | **150.1 ms** |
| 0 dB | 112.5 ms | 108.4 ms | 101.9 ms | **108.9 ms** |
| 5 dB | 83.6 ms | 90.2 ms | 80.3 ms | **74.4 ms** |
| 10 dB | 79.4 ms | 74.6 ms | 72.3 ms | **68.7 ms** |
| 15 dB | 75.5 ms | 83.1 ms | 70.3 ms | **72.2 ms** |
| 20 dB | 84.2 ms | 86.9 ms | 75.4 ms | **74.6 ms** |

| SNR | 单任务(KAN MSE) r | w_eq r | w_t60 r | **w_denoise r** |
|---|---|---|---|---|
| −5 dB | 0.839 | 0.879 | 0.831 | **0.898** |
| 0 dB | 0.952 | 0.953 | 0.959 | 0.951 |
| 5 dB | 0.973 | 0.966 | 0.972 | **0.977** |
| 10 dB | 0.973 | 0.974 | 0.975 | **0.976** |
| 15 dB | 0.979 | 0.972 | 0.980 | **0.979** |
| 20 dB | 0.972 | 0.968 | 0.975 | **0.975** |

### 6.2 去噪按 SNR 分 bin（w_denoise，test1–test4 均值）

> 注：去噪评估结果仅有测试集整体指标，无 SNR 分 bin 数据（`test_multitask_denoise.py` 未实现分 bin 统计）。
> 以下为 4 个测试集的逐集结果，各测试集 SNR 分布不同（test1/2 为 seen noise，test3/4 为 unseen noise）。

| 测试集 | PESQ（增强） | STOI（增强） | SI-SDR（增强） |
|---|---|---|---|
| test1（模拟 RIR + seen noise） | 3.582 | 0.932 | 21.77 dB |
| test2（真实 RIR + seen noise） | 3.572 | 0.934 | 21.89 dB |
| test3（模拟 RIR + unseen noise） | 3.580 | 0.937 | 21.72 dB |
| test4（真实 RIR + unseen noise） | 3.597 | 0.943 | 21.94 dB |

---

 ## 7. 梯度分析摘要

对共享层（encoder、tscb_1、tscb_2）的梯度探针结果（全训练过程均值）：

| 实验 | cos 均值 | neg% | r 均值（‖g_T60‖/‖g_denoise‖） |
|---|---|---|---|
| w_denoise | +0.030 | ~37% | **0.33** |
| w_balanced | +0.028 | ~41% | 0.89 |
| w_eq | +0.031 | ~40% | 0.93 |
| w_t60 | +0.024 | ~42% | 1.83 |

### 指标含义

**cos（余弦相似度）**：衡量 denoise 梯度 g₁ 与 T60 梯度 g₂ 的方向关系，取值 [-1, +1]。

| cos 区间 | 含义 |
|---|---|
| ≈ +1 | 完全协同，两任务梯度方向一致 |
| (0, +1) | 正协同，互相"帮忙"，加速收敛 |
| ≈ 0 | 正交，互不干扰，各自独立更新 |
| (-1, 0) | 负协同（冲突），更新方向部分对立，相互抑制 |
| ≈ -1 | 完全对抗，一个任务的进步完全破坏另一个任务 |

**neg%**：每个 probe 窗口内 cos < 0 的步数占比，取值 [0%, 100%]。与 cos 均值互补——cos 均值接近 0 可能是大量正交步骤，也可能是大量 +1 和 -1 相互抵消（高 neg% + 高 |cos|），neg% 区分了这两种情况。

| neg% 区间 | 含义 |
|---|---|
| < 20% | 极少冲突，任务高度协同 |
| 20%–40% | 偶发冲突，整体协同 |
| 40%–50% | 接近正交，轻度震荡 |
| ≈ 50% | 完全随机，两任务梯度方向无关联 |
| > 50% | 系统性冲突，负方向更常见 |
| > 80% | 严重对抗，任务根本性排斥 |

**r（梯度幅度比）**：r = ‖g_T60‖ / ‖g_denoise‖，衡量哪个任务对共享层的"拉力"更强。

| r 区间 | 含义 |
|---|---|
| r ≪ 1（如 0.1–0.4） | denoise 主导共享层，T60 作为从属 |
| r ≈ 1 | 两任务平等竞争共享层 |
| r ≫ 1（如 2+） | T60 主导，共享层为 T60 服务 |

### 结果解读

- **cos ≈ +0.03，neg% ≈ 37–41%**：三组实验两任务梯度均接近正交，无系统性对抗冲突。这排除了使用梯度修复（GR）的必要性——GR 针对的是对抗型冲突（cos < 0 且 neg% > 50%），在正交型冲突下强行投影反而会压制 denoise 对 T60 的正向正则化效果。

- **r 是决定因素**：三组实验 cos 均值几乎相同，但 r 差异显著，与 T60 性能的排序完全一致。denoise 的强监督信号迫使共享层学习高质量时频表征；T60 Head 在此基础上精准拟合，性能反而最优（r=0.33）。反之，T60 梯度反超（r=1.83）时共享层退化为"T60 专用"，丰富表征消失，两任务均变差。

- **核心结论**：在复杂任务（denoise）+ 简单任务（T60）的组合中，**让复杂任务主导共享层（r < 1），简单任务作为附属**是最优策略。r 比 cos 更能预测最终性能。

---

## 8. 结论

1. **多任务有效**：引入去噪辅助任务后，T60 估计最优 RMSE 从单任务 kan_v2_mse 的 111.15 ms 降至 96.41 ms（−13.3%），代价是新增两个去噪解码器（约 0.54M，+63%）
2. **权重比例关键**：四组实验性能排序（w_denoise > w_balanced > w_eq > w_t60）与梯度幅度比 r 排序完全一致（0.33 < 0.89 < 0.93 < 1.83），证明 r 是决定因素
3. **机制**：去噪强监督作为共享层的正则化器，梯度方向近似正交（cos≈+0.03，无系统性冲突），主要影响因素是梯度幅度比 r 而非方向冲突；让复杂任务（denoise）主导共享层，简单任务（T60）反而受益
4. **去噪质量可用**：w_denoise 平均 PESQ = 3.58，w_balanced 平均 PESQ = 3.41，在中高 SNR 场景下增强语音主观可听性良好
5. **单任务为消融基线**：单任务 kan_v2_mse（RMSE=111.15 ms）作为控制变量基线证明去噪辅助任务的增益；四组多任务实验中只有 w_t60（T60梯度过强）退化至与单任务相近水平
6. **外部对比**：w_denoise（96.41 ms）显著优于 BERP（151.93 ms）、DAMTL（138.94 ms）、noiseaware（133.66 ms）
