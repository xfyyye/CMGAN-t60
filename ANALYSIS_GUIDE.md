# 实验分析结果解读指南

> 本文从零开始解释：网络结构是什么、每张图在测量什么、看到的现象说明了什么。

---

## 一、先搞清楚网络在做什么

### 1.1 任务背景

我们有一段**含噪混响的语音**，想同时做两件事：

- **去噪（Denoise）**：把噪声去掉，还原出干净的混响语音
- **T60 估计**：从这段语音里预测房间的混响时间（单位毫秒，越准越好）

关键问题是：**这两件事能互相帮忙吗？**

### 1.2 网络结构

```
输入: 含噪混响语音的频谱图 (B, 2, T, F)
          │
    ┌─────▼──────┐
    │  Encoder   │  ← 底层：提取最基础的时频特征
    └─────┬──────┘
          │
    ┌─────▼──────┐
    │  TSCB-1    │  ← 中层：跨时间/频率的长程依赖建模
    └─────┬──────┘
          │
    ┌─────▼──────┐
    │  TSCB-2    │  ← 高层：更抽象的语音表征
    └──────┬─────┘
           │
     ┌─────┴──────┐
     │            │
  T60 Head    Denoise Head
  (估计混响时间)  (重建干净语音)
```

**共享层**（Encoder + TSCB-1 + TSCB-2）同时服务于两个任务。
两个任务的学习信号（梯度）都会流回来修改共享层的参数。

### 1.3 四个对比实验

训练时，总损失 = α × 去噪损失 + β × T60损失，通过调整 α/β 控制哪个任务主导：

| 实验名 | α（去噪权重） | β（T60权重） | 谁主导 |
|---|---|---|---|
| **w_denoise** | 1.0 | 0.1 | 去噪强，T60弱 |
| **w_balanced** | 0.5 | 0.5 | 均衡 |
| **w_eq** | 1.0 | 1.0 | 均衡但总体更强 |
| **w_t60** | 0.1 | 1.0 | T60强，去噪弱 |

还有一个**单任务基线**（只做T60估计，没有去噪任务），用于对比"加入去噪究竟有没有用"。

---

## 二、梯度分析：训练过程中两个任务在"争抢"什么

> 图表位置：`figures/gradient/`

### 2.1 基本概念：什么是梯度

神经网络训练时，每个参数更新方向叫**梯度**。多任务模型里，去噪任务和T60任务各自会产生一个梯度，共享层的参数要同时响应这两个梯度。

两个梯度的关系有三种情况：
- **协同**：方向一致，两个任务在"合力"推动参数往同一方向走
- **正交**：方向垂直，互不干扰，各更新各的维度
- **对抗**：方向相反，一个任务的进步会破坏另一个任务

---

### 2.2 图1：`cos_timeseries.png` — 梯度方向的时间序列

**这张图测量什么：**

横轴是训练步数，纵轴是两个任务梯度方向的余弦相似度（cos）。
cos 的含义：+1 = 完全协同，0 = 正交，-1 = 完全对抗。

**怎么看：**

- 绿色背景区（cos > 0）= 协同区
- 红色背景区（cos < 0）= 冲突区
- 四条线都紧贴在 0 附近，幅度约 ±0.1

**说明了什么：**

四组实验在整个训练过程中，去噪梯度和T60梯度的方向**几乎始终接近正交**，既没有明显协同，也没有持续冲突。两个任务"和平共处"，各自学各自的，不互相拖后腿。

> 注意：这张图四条线几乎重叠，说明 **cos 不是区分四组实验性能差异的关键因素**。

---

### 2.3 图2：`r_timeseries.png` — 梯度幅度比的时间序列

**这张图测量什么：**

纵轴是 r = ‖T60梯度‖ / ‖去噪梯度‖，即两个任务对共享层"施力"的强度之比。
- r < 1：去噪任务"拉力"更强，主导共享层的更新
- r > 1：T60任务"拉力"更强，主导共享层的更新
- 虚线（r=1）= 两任务平等

**怎么看：**

- 蓝线（w_denoise）**始终在虚线以下**，稳定在 0.3~0.4，去噪始终主导
- 红线（w_t60）**始终在虚线以上**，经常在 1.5~2.5，T60始终主导，且波动大
- 绿线（w_balanced）和橙线（w_eq）在虚线上下徘徊

**说明了什么：**

这张图和图1形成对比——方向（cos）四组几乎相同，但**幅度（r）差异巨大且稳定**。这是决定最终性能的关键。

---

### 2.4 图3：`cos_r_bar.png` — 四组实验均值对比（四宫格）

**怎么看四个子图：**

**子图A（cos 均值）：** 四组都约 +0.03，几乎没有差别。确认 cos 不是性能差异的来源。

**子图B（r 均值）：** 从左到右：0.33 → 0.89 → 0.93 → 1.83，差异显著。

**子图C（neg% 冲突比例）：** 四组都约 38~42%，都低于 50%（红色虚线），说明没有系统性对抗冲突。这也解释了为什么不需要梯度修复（GR）——GR 是为 neg% > 50% 的对抗型冲突设计的。

**子图D（r vs T60 RMSE 散点图）：** 这是最直接的因果关系图。四个点排成一条趋势线，Pearson r = 0.885，说明 **r 越大，T60 估计越差**，两者高度相关。

---

### 2.5 图4：`cos_hist.png` — cos 分布直方图

**这张图测量什么：**

把训练过程中所有 cos 采样值画成分布图，看分布形状。

**有两种"均值接近0"的情况需要区分：**
- **正交型**：大多数步骤 cos 就在 0 附近，分布集中（窄峰）
- **抵消型**：一半步骤 cos = +1，一半 cos = -1，平均下来是0，分布展开（双峰或宽峰）

**怎么看：**

四张图都是窄高的单峰，集中在 0 附近，标注为 "orthogonal (cos≈0, low std)"。

**说明了什么：**

这证明两任务的关系是**真正的正交**，而不是剧烈对抗相互抵消。两个任务在梯度方向上确实不互相干扰。

---

### 2.6 图5：`layer_cos_r.png` — 分层梯度分析

**怎么看：**

左图：三层（Encoder、TSCB-1、TSCB-2）的 cos 均值，四组实验都几乎没有差别。

右图：三层的 r 均值。规律很清晰：
- w_denoise（蓝色）在**所有三层都维持 r ≈ 0.3**，去噪全面主导
- w_t60（红色）在**所有三层都维持 r ≈ 1.5~1.8**，T60全面主导

**说明了什么：**

权重比例（α/β）对梯度幅度比的影响是**从底层到高层一致渗透**的，不是只影响某一层。w_denoise 的去噪监督在整个 backbone 中都保持主导地位。

---

## 三、表征分析：共享层到底学到了什么

> 图表位置：`figures/representation/`

### 3.1 三种分析方法的直觉解释

**线性探针**：冻结模型参数，把某一层的输出接一个简单的线性回归，看能不能预测T60。能预测得越准（R² 越高），说明这一层**已经把T60信息编码进去了**。

**t-SNE**：把高维特征降到 2D，每个点是一个样本，颜色代表真实T60值。如果 T60 相近的点在图上也聚在一起，说明特征空间里**T60有结构**。

**CKA**：比较两个模型（单任务 vs 多任务）在同一层的特征有多相似。接近1 = 两模型学到了几乎一样的特征；接近0 = 完全不同的特征。

---

### 3.2 图6：`linear_probe.png` — 线性探针 R²

**怎么看：**

每组柱子代表一层，柱子高度是 R²（0~1，越高说明该层包含越多**可被线性回归直接读出的** T60 信息）。

**现象：**

| 层 | 单任务 | w_denoise | w_balanced | w_t60 |
|---|---|---|---|---|
| Encoder | 0.39 | **0.42** | 0.26 | 0.03 |
| TSCB-1 | **0.68** | 0.52 | 0.49 | 0.38 |
| TSCB-2 | **0.65** | 0.58 | 0.59 | 0.61 |

**有两个现象需要分开解读，不能混为一谈：**

**现象 A（Encoder 层）：** w_denoise（0.42）> 单任务（0.39）> w_t60（0.03）。
去噪强监督在底层确实帮助建立了更好的时频感知能力。w_t60 的 Encoder 几乎废了，说明纯 T60 标量回归损失无法驱动底层学习细粒度特征。

**现象 B（TSCB 层）：** 单任务反而最高。这里需要特别注意——线性探针 R² 高，测量的是"T60 信息是否线性可分"，而不是"最终估计精度是否更高"。两者是不同的事。

单任务模型专门为 T60 优化，所以它的 TSCB 层特征对 T60 "线性友好"——T60 信息被直接摆在表面，线性回归一读就准。

w_denoise 的 TSCB 特征是为去噪+T60 共同优化的，T60 信息被**编码进了更复杂的非线性特征结构**里，线性探针探不到，但 T60 Head（本身是非线性的 KAN 网络）能从中提取出更准确的估计。

**一句话：** TSCB 层线性探针高 ≠ 最终效果好。单任务把 T60 信息摆在表面（线性可分）但上限低；w_denoise 把更丰富的时频特征留在 TSCB 层，非线性的 T60 Head 能从中挖出更准确的结果。

**线性探针真正有力的结论只在 Encoder 层：** w_t60 的 Encoder R²=0.03 证明 T60 标量损失无法驱动底层学习有效特征；w_denoise 的 Encoder R²=0.42 超过单任务，证明去噪监督从底层就建立了更好的时频感知。

---

### 3.3 图7/8：`tsne_tscb_2.png` / `tsne_encoder.png` — t-SNE 可视化

**怎么看：**

每个点代表一个音频样本，颜色从深紫（T60=0.1s，短混响）到亮黄（T60=1.5s，长混响）。理论上如果颜色有平滑的空间渐变，说明特征空间里 T60 被有序编码了。

**现象：**

四张图颜色分布**几乎看不出明显差异**，无法用来区分哪个模型更好。

**为什么 t-SNE 在这里不适用：**

t-SNE 是为**离散类别**（如不同类别的图片分开聚类）设计的，擅长展示"有没有团"。而 T60 是连续值（0.1~1.5s），其在特征空间里的结构是渐变的流形，不是离散的团。对于连续值问题，t-SNE 可视化本来就很难体现模型间的细微差异。

**这两张图的结论：** 无法从 t-SNE 看出 w_denoise 相对于单任务的优势，这是工具选择的局限，不是模型没有差异。定量结论要靠线性探针（R²）和最终 RMSE，而不是 t-SNE。

---

### 3.5 图9：`cka_heatmap.png` — CKA 特征相似度热力图

**怎么看：**

- 行 = 层（Encoder → TSCB-1 → TSCB-2，从浅到深）
- 列 = 三个多任务模型
- 颜色：深绿 = 与单任务很相似（CKA接近1），黄色 = 与单任务差异大（CKA接近0）

**现象：**

| 规律 | 数据支撑 |
|---|---|
| 越深层，多任务模型与单任务差异越大 | Encoder行（深绿）→ TSCB-2行（浅黄） |
| w_denoise 与单任务差异最大 | w_denoise列在TSCB-2行=0.468，最浅 |
| w_t60 与单任务最相似 | w_t60列整体最深 |

**解释：**

w_denoise 的 TSCB-2 特征与单任务相差最大（CKA=0.468），说明去噪强监督**从根本上重塑了高层特征**，这些新特征不是单任务能学到的。而 w_t60 特征跟单任务几乎一样（0.799），等于"加了T60权重但啥都没改变"。

---

## 四、把所有证据串起来

> 从"为什么加去噪能让T60估计更好"这个问题出发，所有分析形成一条完整的因果链。

```
加入去噪辅助任务
      │
      ▼
去噪任务损失对共享层施加了强梯度
（α=1.0 时，去噪梯度幅度是T60的3倍：r=0.33）
      │
  ┌───┴────────────────────────────────────┐
  │ 梯度方向没有冲突（cos≈0，neg%<50%）    │
  │ → 两任务和平共存，去噪帮了T60          │
  │   而不是把T60"挤走"                   │
  └───────────────────────────────────────┘
      │
      ▼
去噪监督迫使 Encoder 学习精细的时频判别特征
（噪声 vs 混响 → 必须理解时频能量的细节）
      │
  [证据1: 线性探针 Encoder 层]
  w_denoise Encoder R²=0.42 > 单任务 0.39
  → 底层已经建立了更好的时频感知能力
  （注：TSCB 层单任务 R² 更高，但那测的是"线性可分性"
    不等于最终精度，T60 Head 是非线性网络，
    能从非线性特征结构中挖出更准确的估计）
      │
      ▼
这些特征在 TSCB 层进一步被去噪任务重塑
      │
  [证据2: CKA]
  w_denoise TSCB-2 CKA=0.468（与单任务差异最大）
  → 高层特征被根本性地重组为去噪+T60联合表征
      │
      ▼
T60 Head 在更好的特征基础上做回归
      │
  [最终结果]
  T60 RMSE: 107.70ms（单任务）→ 96.41ms（w_denoise）
  提升 10.5%
```

**反例（w_t60）的因果链：**

```
α=0.1，β=1.0：T60梯度幅度是去噪的18倍（r=1.83）
      │
      ▼
T60 损失（稀疏标量回归）主导共享层更新
      │
  [证据: 线性探针]
  w_t60 Encoder R²=0.03（几乎为零）
  → 底层特征退化，失去时频分析能力
      │
      ▼
[证据: CKA]
w_t60 TSCB-2 CKA=0.799（与单任务几乎一样）
→ 特征没有被重塑，多任务没有带来新东西
      │
      ▼
T60 RMSE: 107.84ms（几乎等于单任务107.70ms）
加了任务，没有收益
```

---

## 五、一句话总结

**"让去噪任务主导共享层（梯度幅度比 r ≈ 0.3），而不是让T60任务主导，反而能让T60估计得更准。"**

原因：去噪是一个复杂、信息密集的任务，它被迫让共享层学习精细的时频表征；T60估计是一个简单的标量回归，它的梯度没有能力驱动底层特征学习这种精细结构。当复杂任务主导共享层时，简单任务"搭便车"坐享其成，效果反而更好。

---

## 附：图表速查表

| 图表 | 目录 | 一句话说明 |
|---|---|---|
| `cos_timeseries.png` | `figures/gradient/` | 训练全程梯度方向，四组均接近正交 |
| `r_timeseries.png` | `figures/gradient/` | 梯度幅度比随时间变化，w_denoise始终最低 |
| `cos_r_bar.png` | `figures/gradient/` | 四组均值对比+r与RMSE的散点图（核心结论图） |
| `cos_hist.png` | `figures/gradient/` | 证明是正交型而非抵消型冲突 |
| `layer_cos_r.png` | `figures/gradient/` | 三层分层对比，r的影响从底层到高层一致 |
| `linear_probe.png` | `figures/representation/` | 各层T60信息量，w_denoise底层最强 |
| `tsne_tscb_2.png` | `figures/representation/` | 高层特征可视化（对连续T60值区分度有限，仅供参考） |
| `tsne_encoder.png` | `figures/representation/` | 底层特征可视化（同上，t-SNE不适合连续值比较） |
| `cka_heatmap.png` | `figures/representation/` | 与单任务的特征差异，w_denoise重塑最彻底 |
